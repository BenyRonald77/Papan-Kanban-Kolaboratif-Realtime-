DATABASE_URL="file:./dev.db?connection_limit=1&socket_timeout=20"
JWT_SECRET="ganti-dengan-secret-acak-yang-panjang"

# Port HTTP + WebSocket (satu server, satu port)
PORT=3000

# Interval (ms) debounce penyimpanan snapshot Yjs ke database
SNAPSHOT_DEBOUNCE_MS=2000
